<% if (typeof errors != 'undefined') { %>
	<% errors.forEach(function(error) { %>
		<%= error.msg %>
	<% }); %>
<% } %>

<% if (success_msg != '') { %>
	<%= success_msg %>
<% } %>

<% if (error_msg != '') { %>
	help
	<%= error_msg %>
<% } %>

<% if (error != '') { %>
	hello
	<%= error %>
<% } %>